---
import { site } from "@config/site";
import type { MenuItem } from "./Menu";
import { Menu } from "./Menu";
import Grid from "@components/Grid.astro";
import Section from "@components/Section.astro";
import Typography from "@components/Typography.astro";

interface FooterMenu {
  title: string;
  menu: MenuItem[];
}

const menus: FooterMenu[] = [
  {
    title: "Delta Lake",
    menu: site.menus.footerMain,
  },
  {
    title: "Learn",
    menu: site.menus.footerLearn,
  },
  {
    title: "Community",
    menu: site.menus.footerCommunity,
  },
  {
    title: "Social Channels",
    menu: site.menus.social,
  },
];
---

<footer class="text-darkTextSecondary">
  <Section padding="xl">
    <Grid
      columns={{ xs: 1, lg: ["133px", "auto", "250px"] }}
      className="gap-y-lg lg:gap-x-xl"
    >
      <div>
        <img
          src="/images/delta-lake-logo.svg"
          alt="Delta Lake"
          width="133"
          height="28"
          loading="lazy"
        />
      </div>
      <div>
        <Grid columns={{ xs: 1, lg: menus.length }} className="gap-y-lg">
          {
            menus.map(({ title, menu }) => (
              <div>
                <div class="font-bold uppercase mb-sm">{title}</div>
                <Menu
                  items={menu}
                  itemClassName="text-white"
                  menuClassName="gap-xs"
                />
              </div>
            ))
          }
        </Grid>
      </div>
      <div>
        <Typography variant="p2" className="mb-md">
          Copyright © {new Date().getFullYear()} Delta Lake, a series of LF Projects,
          LLC. For web site terms of use, trademark policy and other project polcies
          please see{" "}
          <a href="https://lfprojects.org" target="_blank">
            https://lfprojects.org
          </a>.
        </Typography>
        <img
          src="/images/the-linux-foundation-logo.svg"
          alt="The Linux Foundation"
          width="100"
          loading="lazy"
        />
      </div>
    </Grid>
  </Section>
</footer>
